# Welcome to Slidev!

To start the slide show:

- `npm install` (or `pnpm install`)
- `npm run dev` (or `pnpm dev`)
- visit http://localhost:3030

Edit the `slides.md` to see changes. Learn more at https://sli.dev/

